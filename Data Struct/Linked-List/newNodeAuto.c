#include <stdio.h>
#include <stdlib.h>


typedef struct node { 
    int data;
    struct node *next;
}Nodee;

void newNode(int noNode, Nodee *head){
    struct node *newNode = NULL;

    for(int x=1;x<=noNode;x++){
        
    }

    
    
}

int main(){

    int noNode;
    printf("Enter The Input of Node");
    scanf("%d", &noNode);

    Nodee *head = NULL;
    newNode(noNode, head)


}